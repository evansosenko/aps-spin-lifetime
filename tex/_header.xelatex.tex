% For compiling with XeLaTeX,
% put anything here that should be included in the header.

% UTF-8 math support
\usepackage{unicode-math}

% PDF bookmarks.
\usepackage[hidelinks,colorlinks]{hyperref}

% Enables the \cref command.
\usepackage[noabbrev]{cleveref}
